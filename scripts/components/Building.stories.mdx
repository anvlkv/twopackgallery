import { Meta, Story } from "@storybook/addon-docs";
import Building from "./Building";

<Meta title="Building" component={Building} />

export const Template = (args) => <Building {...args} />;

<Story
  name="Building"
  args={{
    atrium:[8, 20, 10],
    sides: {
      'y': [[7.7, 1, 9.7], 7, [1, 10, 1]],
      'z': [[2, 10, 2], [3, 18, 2]],
      '-z': [[2, 10, 2], [3, 18, 2]],
      '-y': [[14, 2, 10]]
    }
  }}
>
  {Template.bind({})}
</Story>
